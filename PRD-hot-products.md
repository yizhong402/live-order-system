# 首页热销品 & 商品管理新上架 — 需求文档

---

## 1. 背景

当前系统有 3199 个商品，每天 OMS 定时校准库存。但没有历史库存记录，无法判断哪些商品卖得快（热销）或哪些是新上架的。需要补充库存快照机制来实现这两个功能。

> ⚠️ 快照机制**完全独立**，不碰 OMS 校准逻辑。

---

## 2. 库存快照机制

### 2.1 怎么做

- 新增一个 Python 脚本 `stock_snapshot.py`
- 每天早上 **08:00** 通过 cron 自动执行
- 脚本只做一件事：**读取 BaaS `products` 表的所有 SKU 和库存，照原样存到本地 JSON 文件**

### 2.2 数据格式

```
data/stock-snapshots.json

{
  "2026-06-25": {
    "BA0125111905": 2,
    "A6-GRID-0509": 50,
    "BA0125112410": 100,
    ...
  },
  "2026-06-26": {
    "BA0125111905": 1,    ← 卖掉了1件
    "A6-GRID-0509": 45,   ← 卖掉了5件
    "BA0125112410": 150,  ← 新入库了50件（负数变动，不算热销）
    "NEW-SKU-001": 80,    ← 新上架（昨天没有这个SKU）
    ...
  }
}
```

### 2.3 今天先跑一次

现在立即跑一次脚本，生成 `2026-06-25` 的快照。明天 8:00 自动拍 `2026-06-26` 的，就有对比数据了。

---

## 3. 首页：🔥 热销品 Top 20

### 3.1 位置

替换当前首页"库存概览"板块。

### 3.2 热销度定义

```
热销度 = 前一天库存 - 当天库存   (只看正数)
```

例如：
| SKU | 6/25库存 | 6/26库存 | 变动 | 是否热销 |
|-----|---------|---------|------|---------|
| A | 100 | 50 | -50 ✅ | 热销第1 |
| B | 80 | 30 | -50 ✅ | 热销第2 |
| C | 60 | 60 | 0 | 不变，不管 |
| D | 20 | 25 | +5 ❌ | 负数，不算 |

### 3.3 展示内容

每项显示（按变动量从大到小排序，取前20）：

```
[产品图片]  BA0125111905  A8成品【包含全套内页+书皮】
           📉 昨日减少：50件  ████████████░░░░░░░░░░  50/100
```

- 图片 → 来自 `products` 表的 `image_url`
- SKU → 来自 `products` 表的 `sku`
- 名称 → 来自 `products` 表的 `name`
- 进度条宽度 = 变动量 / 前一天库存 × 100%，颜色越红代表减少越多
- 不足 20 个热销品则全显示
- 销量为 0 时显示"暂无热销数据"

### 3.4 刷新

首页 "🔄 刷新" 按钮重新加载快照数据。

---

## 4. 商品管理：🆕 新上架产品

### 4.1 位置

商品管理页（`page-product`）顶部新增一个板块，放在搜索筛选区上方。

### 4.2 新上架定义

```
当天快照库存 > 0 且 前一天快照库存 = 0（或昨天没有这个SKU）
```

### 4.3 功能需求

| 功能 | 说明 |
|------|------|
| **日期筛选** | 下拉选择日期（只有有快照的日期才可选），查看该天的新上架产品 |
| **排序** | 按 **变动量**（库存数）正序/倒序排列 |
| **显示内容** | 产品图片 + SKU + 名称 + 当前库存 + ¥价格 + $价格 |
| **每行样式** | 卡片式，和商品列表风格一致 |
| **无数据** | 显示"该日期暂无新上架产品" |

---

## 5. 文件改动清单

| 文件 | 动作 | 说明 |
|------|------|------|
| `stock_snapshot.py` | **新增** | 每天8点跑，读 BaaS products → 写本地 JSON |
| `data/stock-snapshots.json` | **新增** | 快照数据文件 |
| `index.html` | **修改首页** | 库存概览 → 热销品 Top 20 |
| `index.html` | **修改商品页** | 新增"新上架产品"板块 |
| 前端 JS（app.js） | **新增函数** | `loadStockSnapshots()` 加载 + 对比快照 |
| cron | **新增** | `0 8 * * * cd /tmp/live-order-system && python3 stock_snapshot.py` |
| HTTP 静态文件服务 | **调整** | 新增 `/data/` 目录的静态文件服务 |

---

## 6. 不做的事（明确边界）

- ❌ **不碰 OMS 校准逻辑**
- ❌ **不改 `oms_sync.py` 文件**
- ❌ **不断开任何现有功能**
- ❌ **不用手机工具/通知**
- ❌ **不在 BaaS 建新表**（BaaS 不允许自动建表）

---

核对一下，有没有遗漏或需要调整的？
