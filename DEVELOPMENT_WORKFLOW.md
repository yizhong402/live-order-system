# 开发工作流程（标准化版）

> 目标：减少来回修改，一次做对。
> 
> 来源：本工具开发过程中 9 轮修复 + 反复踩坑的经验总结。

---

## 核心原则

1. **先写清楚再动手** — 需求一句话和需求一页纸，做出来差很远
2. **改之前先评估影响面** — "改一行"可能影响十个模块
3. **改完必须完整走一遍主流程** — 不是只测改的地方
4. **提交信息说清楚"为什么"** — 方便以后回滚和溯源
5. **数据写入必须想清楚"写时序"** — 先写哪、后写哪、写失败怎么办、用户以为写完了实际写完了没

---

## 流程总览

```
需求分析 → 技术方案 → 代码修改 → 单元验证 → 集成测试 → 提交
                     ↑_____________←______←______← 发现问题回到修改
                                                     (最多循环1次)
```

---

## 一、需求分析阶段

### 做什么
把用户的需求转化成清晰、无歧义的任务描述。

### 产出
- **需求清单**（按优先级排列）
- **每项的验收标准**（怎么才算"做好了"）

### 检查清单
- [ ] 这个需求解决了什么实际问题？
- [ ] 有没有更简单的方案？
- [ ] 不做会怎样？（判断要不要做）
- [ ] 有没有类似的现有功能可以复用？
- [ ] **验收标准是客观的、可操作的？**（避免"感觉好了"）

### 踩坑记录
> **踩过坑**：用户说"做个备份功能"，我的第一反应写代码。但实际涉及：存哪里(BaaS不支持建表)、图片太大(BaaS字段限制)、跨电脑恢复(需要文件导出兜底)。如果先花5分钟想清楚方案，就不会写了一半翻工。

---

## 二、技术方案阶段

### 做什么
确定"怎么做"：选什么方案、涉及哪些文件、影响哪些功能。

### 产出
- 修改方案（修改哪些文件、改什么、怎么改）
- 风险评估（可能影响到的功能列表）
- 兜底方案（如果方案不可行，Plan B是什么）

### 检查清单
- [ ] 方案涉及的所有文件都列全了？
- [ ] 是否考虑了**边界情况**？（空数据、大数、非法输入、网络异常）
- [ ] **数据格式是否确认过？**（BaaS返回什么格式？哪个字段？）
- [ ] 有没有**兼容旧数据**？（已有数据和新逻辑是否兼容？）
- [ ] 这个改动会影响已有功能吗？（回归影响评估）
- [ ] 是否考虑了**性能**？（数据量大的时候会不会卡死？）
- [ ] ⏱ **数据写入时序是否明确？**（参考下方"写时序规则"）

### ⏱ 数据写入时序规则（本工具专用）

这个工具的特殊性：**直播场景不能卡 UI，但数据不能丢**。

#### 写时序决策树

每次涉及"写入数据"时，按顺序回答以下问题：

```
┌─ 这个操作用户是否在等结果？ ──────────┐
│    是（直播场景，不能卡）              │
│    → 先写 localStorage（即时）         │
│    → BaaS 异步写，不 await             │
│    → 用户看到"成功"就继续操作          │
│    → 后台写失败：静默重试 1 次         │
│                                          │
│    否（非直播操作，如设置页）          │
│    → await BaaS 写完成后再弹结果       │
│    → 写入失败 → 明确提示用户           │
└────────────────────────────────────────┘
```

#### 写入介质优先级

| 介质 | 速度 | 持久性 | 用途 |
|------|------|--------|------|
| `localStorage` | 即时 (≈0ms) | 本机（清浏览器会丢） | 快速回显 + 防丢缓冲 |
| BaaS 云端 | 500ms~3s | 持久（换设备也在） | 最终持久化 |

#### 写时序三原则

**原则 1：先落本地，再写云端**
- 用户点击"下一轮" → `orders.push()`（内存即刻可见）→ `localStorage` 异步刷（≈0ms）→ BaaS .then()（约 1s）
- 目的是：用户操作完立刻看到结果，BaaS 在后台慢慢写

**原则 2：明确"写入窗口期"**
- 从"用户看到成功"到"BaaS 确认写完"之间有时间差，称为**写入窗口期**
- 窗口期内：
  - 刷新页面：数据还在 localStorage，BaaS 没写全 → **需要 localStorage 兜底恢复机制**
  - 换设备登录：数据没有 → 只有云端的部分能看到
- **对策**：窗口期尽量短（< 1s），且 localStorage 和 BaaS 双写

**原则 3：写失败不丢用户数据**
- BaaS 写失败 → **不删 localStorage！** 下次打开时，检查 localStorage 是否有未同步数据，补传一次
- 不要因为"云端写失败了"就把本地数据也删了

#### 这个工具的具体写时序图

```
用户点击"下一轮"
    │
    ├─ 1️⃣ 内存：orders.push(order)          ← 即时（毫秒级）
    ├─ 2️⃣ localStorage.setItem('orders', …)  ← 即时（毫秒级，防丢）
    ├─ 3️⃣ UI 更新：列表中显示新订单           ← 即时
    ├─ 4️⃣ 用户看到成功，继续操作               ← 零等待 ✅
    └─ 5️⃣ 后台异步：BaaS insert()            ← 约 1s（失败静默 + console.error）
         │
         ├─ 成功 → 无事
         └─ 失败 → 记录日志，下次打开尝试补传
```

#### 之前踩过的写时序坑

| 问题 | 当时做法 | 正确做法 |
|------|---------|---------|
| nextRound await BaaS → 用户等 1s+ | await 阻塞 UI | 异步 `.then()`，零等待 |
| 写 BaaS 失败 → 订单丢了 | 依赖 BaaS 完成才算成功 | localStorage 先存，BaaS 失败不删本地 |
| 同时快速点下一轮 → 写顺序乱 | 没防并发 | `lock` 锁 + `.then()` 串行 |
| 刷新太快 BaaS 没写完 → 订单丢失 | 仅依赖 BaaS | localStorage 兜底 |

### 踩坑记录
> **踩过坑 #1** — BaaS insert 返回 `{data: 3}`（数字），我以为返回 `{data: {id: 3}}`（对象）。如果先调一次 API 确认返回值，一次就能改对，而不是改 3 轮。
>
> **踩过坑 #2** — 日期格式：用户浏览器 zh-CN locale 返回 `2026/6/24`（无零填充），我的代码只处理了 `2026-06-24`。如果早想到中文浏览器格式差异，一次就能兼容。

---

## 三、代码修改阶段

### 做什么
按技术方案写代码，一次只改一个逻辑单元。

### 规则
1. **小步提交** — 每改完一个功能点就 commit，commit 信息写清楚"为什么改"
2. **不改无关代码** — 一次修改只解决一个问题，顺手改无关代码容易引入新 bug
3. **修改前先读上下文** — 别只看要改的那一行，看它被哪些函数调用、数据怎么流过来的

### 提交信息模板
```
<类型>: <简短描述>

- 问题: (为什么改)
- 方案: (怎么改的)
- 影响: (影响哪些功能/模块)
```

类型：修复 / 新增 / 优化 / 重构

---

## 四、单元验证阶段（改完后立刻做）

### 做什么
每改完一个点，立刻验证这个点对不对。

### 验证清单
| 维度 | 检查内容 |
|------|---------|
| ✅ 功能 | 改的功能能跑通吗？ |
| ✅ 边界 | 空数据、0值、null、undefined 会报错吗？ |
| ✅ 旧数据 | 系统已有数据能兼容吗？ |
| ✅ 报错 | 如果网络出错/接口返回异常，页面会崩溃吗？ |
| ✅ 数据持久化 | 刷新页面后数据还在吗？ |
| ✅ ⏱ **写时序** | 先写什么后写什么？写入窗口期多长？窗口期内刷新会丢吗？ |

### 踩坑记录
> **踩过坑**：改了 BaaS insert 格式后，只测了"写入成功"，没测"刷新后能不能读出"。结果发现 session ID 存的是 Date.now() 格式，BaaS ID 是数字格式，刷新读出来匹配不上，又是一轮修复。

---

## 五、集成测试阶段（改完所有点后做）

### 做什么
完整走一遍核心业务流，确保整个系统是通的。

### 核心功能测试流

这个工具的核心流：

```
创建场次 → 填写竞拍链接信息 → 扫码添加SKU → 下一轮保存 
→ 检查首页看板统计 → 检查毛利计算 → 刷新页面确认持久化
```

每个步骤要验证的：

#### 1️⃣ 创建场次
- [ ] 输入标题、日期、时间、主播、平台 → 能保存
- [ ] 场次出现在选择下拉列表中
- [ ] 刷新后场次仍然存在

#### 2️⃣ 竞拍链接/金额/备注
- [ ] 输入金额、备注 → 保存
- [ ] 多个轮次各自独立显示
- [ ] 刷新后数据还在

#### 3️⃣ SKU 扫码添加
- [ ] 扫描或手动输入 SKU 编码 → 商品信息能调出来
- [ ] 多个 SKU 能正常叠加
- [ ] 编辑已添加的 SKU

#### 4️⃣ 订单保存
- [ ] 点击下一轮 → 订单保存成功
- [ ] 前端列表能看到新订单
- [ ] BaaS 中有记录（刷新后存在）

#### 5️⃣ 首页看板
- [ ] 今日订单数正确
- [ ] 今日 GMV 正确
- [ ] 总订单数 / 总 GMV 正确
- [ ] 场次选中后订单数正确
- [ ] 库存预警数据正确

#### 6️⃣ 毛利计算
- [ ] 订单数据能正确加载
- [ ] 手续费默认值正常
- [ ] 图表（日趋势/场次对比/商品排行）能切换
- [ ] 导出功能正常

#### 7️⃣ 历史记录
- [ ] 所有订单列表显示
- [ ] 按标题/主播/日期筛选正常
- [ ] 编辑/复制/删除正常
- [ ] 列表能滚动（不会固定在几行）

### 测试数据规范

```
测试场次统一命名: test_<内容描述>_v<版本号>
例: test_order_flow_v3  /  test_backup_v1
```

- 测试数据用完要标注，方便清理
- 涉及真实 BaaS 数据（3199 条商品）的测试，不要随意修改

---

## 六、提交 & 推送阶段

### 检查清单
- [ ] commit 信息清楚说明**为什么改**（不是只写"修复bug"）
- [ ] 所有测试数据已清理（测试场次、测试订单、测试 SKU）
- [ ] 本地跑一遍完整核心流，确认没问题
- [ ] 推送前检查：有没有遗漏的 `console.log` 或调试代码
- [ ] git push 正常

---

## 附：本工具开发 9 轮修复复盘（为什么反复改？）

| 轮次 | 问题 | 根因 | 如果能重来…… |
|------|------|------|--------------|
| 1 | BaaS insert 返回数字 | 没确认 API 返回格式 | **先调一次 API 确认返回结构** |
| 2 | 日期格式不兼容 zh-CN locale | 没考虑浏览器环境差异 | **写代码前想到"中文浏览器日期格式不同"** |
| 3 | 刷新后 session 丢失 | 没验证"刷新"场景 | **单元验证加一条"刷新页面试一下"** |
| 4 | 订单数统计不对 | 用 session 内数据而非全局 | **写代码前想清楚数据来源是全局还是局部** |
| 5 | **异步写入慢 → 用户以为没保存** | 用了 await 阻塞 UI | **评估时就想"直播场景不能卡，先落本地再写云"** |
| 6 | 同时点下一轮 → 重复提交 | 没考虑并发操作 | **防重复/防并发是基本操作** |
| 7 | 锁没释放 → 一直卡住 | try 没有 finally | **改锁逻辑必须加 finally 释放** |
| 8 | **nextRound 写入慢影响直播节奏** | await 阻塞 → 用户等 | **异步写入但保留本地回显** |
| 9 | 库存扣减重复 | 预扣 + 存储双份逻辑冲突 | **先看懂现有库存逻辑再改** |

### 根因总结

| 原因 | 占比 | 对策 |
|------|------|------|
| 没确认 API/BaaS 返回格式 | 30% | 技术方案阶段加"确认数据格式"步骤 |
| 没考虑边界/环境差异 | 25% | 单元验证加边界检查清单 |
| ⏱ **没理清写时序（先写哪、后写哪、窗口期多长）** | 20% | 技术方案阶段强制过"写时序决策树" |
| 没验证刷新后数据持久化 | 15% | 集成测试必测"刷新" |
| 并发/异步场景考虑不足 | 10% | 涉及"点击"/"写入"的地方自动防并发 |

---

## 快速开始（一张图）

```
┌─ 需求分析 ──────────────────────────────┐
│  • 写清楚要什么                          │
│  • 定验收标准                            │
│  • 确认需求优先级                        │
└──────────┬──────────────────────────────┘
           ▼
┌─ 技术方案 ──────────────────────────────┐
│  • 确认 API 返回格式                     │
│  • 列齐所有涉及文件                      │
│  • 评估边界情况 + 兼容性 + 性能         │
│  • ⏱ 过一遍"写时序决策树"               │
│     - 直播场景(不能卡) vs 非直播场景     │
│     - 先落本地再写云？还是直接 await？  │
│     - 窗口期多长？刷新会丢吗？          │
│  • 确定兜底方案 (Plan B)                │
└──────────┬──────────────────────────────┘
           ▼
┌─ 代码修改 ──────────────────────────────┐
│  • 小步提交，一条 commit 一个逻辑        │
│  • commit 写清楚"为什么"                 │
│  • 不改无关代码                          │
└──────────┬──────────────────────────────┘
           ▼
┌─ 单元验证 ──────────────────────────────┐
│  • 功能通了吗？                          │
│  • 边界情况呢？                          │
│  • 刷新后还在吗？                        │
│  • 异常输入会崩吗？                      │
│  ⏱ 写入窗口期快速刷，数据丢不丢？      │
└──────────┬──────────────────────────────┘
           ▼
┌─ 集成测试 ──────────────────────────────┐
│  • 完整走一遍核心业务流                  │
│  • 看板/毛利/历史/持久化 都要测         │
│  • 发现问题最多循环 1 次                 │
│  • 还不通 → 整个方案重评估               │
└──────────┬──────────────────────────────┘
           ▼
┌─ 提交 & 推送 ───────────────────────────┐
│  • 清理测试数据                          │
│  • 清理调试代码                          │
│  • git push                              │
└────────────────────────────────────────┘
```
